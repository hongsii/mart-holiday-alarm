spring:
  profiles:
    active: test

---

spring:
  profiles: test
  output:
    ansi.enabled: always
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    org.hibernate.type: trace
