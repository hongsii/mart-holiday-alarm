server:
  port: 8080

---

spring:
  profiles: dev
  output:
    ansi.enabled: always
  jpa:
    hibernate.ddl-auto: update
    show-sql: true
    open-in-view: false
  h2:
    console:
      enabled: false

logging:
  level:
    root: DEBUG
    org.hibernate: DEBUG

---

spring:
  profiles: prod1

server:
  port: 8081

---

spring:
  profiles: prod2

server:
  port: 8082
