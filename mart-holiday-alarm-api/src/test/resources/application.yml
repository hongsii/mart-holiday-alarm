spring:
  profiles:
    active: test

---

spring:
  profiles: test
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  output:
    ansi.enabled: always

# logging.level.org.hibernate.type: trace
